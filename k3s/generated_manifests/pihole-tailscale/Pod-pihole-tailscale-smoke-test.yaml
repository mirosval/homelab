apiVersion: v1
kind: Pod
metadata:
  annotations:
    helm.sh/hook: test
  name: pihole-tailscale-smoke-test
  namespace: pihole-tailscale
spec:
  containers:
    - command:
        - sh
        - -c
        - curl http://pihole-tailscale-web:80/
      image: curlimages/curl
      imagePullPolicy: IfNotPresent
      name: hook1-container
  restartPolicy: Never
  terminationGracePeriodSeconds: 0
